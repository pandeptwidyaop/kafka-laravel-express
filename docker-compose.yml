version: "3"
services:
  laravel:
    build:
      context: .
      dockerfile: laravel.docker
    volumes:
      - ./laravel:/var/www/html
    ports:
      - 9000:4000
  express:
    build:
      context: .
      dockerfile: express.docker
